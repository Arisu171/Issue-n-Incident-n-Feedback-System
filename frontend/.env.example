# Dùng khi chạy web trực tiếp bằng `npm run dev` (không qua docker compose).
# Sao chép thành .env.local. NEXT_PUBLIC_* được nhúng vào bundle nên không chứa bí mật.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
